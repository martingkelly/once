# Summary.
project('solo', 'c',
  version: '1.0',
  license: 'gplv3',
  default_options: [
    'c_std=c11',
    'buildtype=release',
    'warning_level=3',
    'werror=true'])

add_project_arguments([], language: 'c')

executable('solo', 'solo.c', [], install: true)
